export interface Game {
  id: string;
  title: string;
  slug: string;
  description: string;
  embed_url: string;
  controls: any;
  strategy_tips: string;
  difficulty: number;
  featured_date: string;
  created_at: string;
}